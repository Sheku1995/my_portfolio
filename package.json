{
  "name": "my_portfolio",
  "version": "1.0.0",
  "description": "Portfolio site for Sheku Alpha Kamara",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "optimize-images": "node scripts/optimize-images.js",
    "optimize-images:fallback": "node scripts/generate-optimized-fallback.js",
    "start-electron": "electron ."
  },
  "devDependencies": {
    "sass": "^1.66.1",
    "vite": "^5.0.0",
    "sharp": "^0.32.0"
  },
  "dependencies": {
    "firebase": "^10.0.0",
    "electron": "^26.0.0"
  }
}
